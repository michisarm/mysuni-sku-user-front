
export enum MediaType {
  InternalMedia= 'InternalMedia',    // 내부저작 media(panopto)
  InternalMediaUpload ='InternalMediaUpload', // 로컬업로드 media(panopto)-- user에서만 사용
  LinkMedia= 'LinkMedia',        // 외부 media(Yotube..)
  ContentsProviderMedia= 'ContentsProviderMedia'  // 외부 CP media
}
