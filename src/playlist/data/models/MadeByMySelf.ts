export interface MadeByMySelf {
  cardIds: string[];
  commentFeedbackId: string;
  denizenId: string;
  description: string;
  expose: boolean;
  id: string;
  likeFeedbackId: string;
  modifiedTime: number;
  registeredTime: number;
  title: string;
}
