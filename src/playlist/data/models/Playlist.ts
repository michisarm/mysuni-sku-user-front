export interface Playlist {
  cardIds: string[];
  commentFeedbackId: string;
  denizenId: string;
  description: string;
  expose: boolean;
  id: string;
  likeFeedbackId: string;
  modifiedTime: number;
  registeredTime: number;
  title: string;
}
