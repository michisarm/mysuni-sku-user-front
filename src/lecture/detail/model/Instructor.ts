export default interface Instructor {
  name: string;
  department: string;
  category_json: string;
  email: string;
  usid: string;
  employeeId: string;
  photoId: string;
  company: string;
  instructorLearningTime: number;
  lectureTime: number;
  represent: number;
}
